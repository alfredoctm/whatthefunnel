services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.8
    container_name: wtf-clickhouse
    restart: unless-stopped
    ports:
      - "8123:8123"      # HTTP interface
      - "9000:9000"      # Native protocol (used by @clickhouse/client when configured)
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - ./clickhouse/init:/docker-entrypoint-initdb.d:ro
      - ./clickhouse/users.d/wtf-user.xml:/etc/clickhouse-server/users.d/wtf-user.xml:ro
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8123/ping"]
      interval: 5s
      timeout: 3s
      retries: 12       # up to ~60s for cold start
      start_period: 5s

  api:
    build: ./api
    container_name: wtf-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      CLICKHOUSE_URL: http://clickhouse:8123
      CLICKHOUSE_USER: wtf
      CLICKHOUSE_PASSWORD: wtf
      PORT: "3000"
      LOG_LEVEL: info
      NODE_ENV: production
    depends_on:
      clickhouse:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:3000/health"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s

  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    container_name: wtf-web
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      api:
        condition: service_healthy

volumes:
  clickhouse-data:
