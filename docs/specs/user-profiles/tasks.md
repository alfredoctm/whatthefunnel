# User Profiles — Tasks

> Status: SKELETON. Fill in after design. Each task = 1–3 hours, vertical slice.

- [ ] TODO — e.g., "Add `GET /users/:user_id/events` returning JSON, backed by raw ClickHouse query"
- [ ] TODO — e.g., "Add minimal SSR page rendering the event list"
- [ ] TODO — e.g., "Add pagination via `before=<timestamp>` cursor"
- [ ] TODO — e.g., "Smoke test: ingest 100 events for a user, hit profile page, verify all 100 show"
